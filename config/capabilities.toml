# Master Agent v1 capability catalog. Read connectors are available by default
# but remain inactive until selected; capabilities never grant credentials.

[capabilities."bitbucket.branch.push"]
enabled = false
authentication = "local_git"
risk = "reversible_write"
reversible = true
requires_expected_version = true
description = "Disabled until every local Git metadata transaction is descriptor-bound."

[capabilities."bitbucket.build_status.read"]
enabled = true
authentication = "configured_connector"
risk = "read_only"

[capabilities."bitbucket.instance.read"]
enabled = true
authentication = "configured_connector"
risk = "read_only"

[capabilities."bitbucket.public_repository.list"]
enabled = true
authentication = "anonymous_or_configured_connector"
risk = "read_only"
description = "List public repositories in a specified Bitbucket Cloud workspace without requiring credentials."

[capabilities."bitbucket.pull_request.create"]
enabled = true
authentication = "configured_connector"
risk = "reversible_write"
reversible = true
target_resource_types = ["pull_request"]
parameter_schema = { title = "string", source_branch = "string", destination_branch = "string", description = "string?", close_source_branch = "boolean?", workspace = "string?", repository = "string?", project_key = "string?", repository_slug = "string?" }
description = "Create and verify a Bitbucket pull request; recovery requires a manual re-read and decline."

[capabilities."bitbucket.pull_request.diffstat"]
enabled = true
authentication = "configured_connector"
risk = "read_only"

[capabilities."bitbucket.pull_request.merge"]
enabled = false
authentication = "service_or_delegated"
risk = "high_impact"
reversible = false
description = "Intentionally prohibited from the v1 autonomous execution surface."

[capabilities."bitbucket.pull_request.read"]
enabled = true
authentication = "configured_connector"
risk = "read_only"

[capabilities."bitbucket.pull_request.search"]
enabled = true
authentication = "configured_connector"
risk = "read_only"

[capabilities."bitbucket.repository.read"]
enabled = true
authentication = "configured_connector"
risk = "read_only"

[capabilities."confluence.page.compensate"]
enabled = true
authentication = "configured_connector"
risk = "reversible_write"
reversible = true
requires_expected_version = true
provider_precondition = "version"
target_resource_types = ["page"]
parameter_schema = { title = "string", body = "string", representation = "string?", status = "string?", space_key = "string?", version_message = "string?", source_bindings = "object?" }
description = "Restore the prior Confluence page version and body."

[capabilities."confluence.page.create"]
enabled = true
authentication = "configured_connector"
risk = "reversible_write"
reversible = true
target_resource_types = ["page"]
parameter_schema = { title = "string", body = "string", representation = "string?", status = "string?", space_id = "string?", space_key = "string?", parent_id = "string?", source_bindings = "object?" }
description = "Create a Confluence page; deletion recovery requires manual re-review because the adapter lacks atomic delete preconditions."

[capabilities."confluence.page.create.draft"]
enabled = true
authentication = "local"
risk = "local_generation"
max_input_bytes = 4194304
max_output_bytes = 16777216
target_resource_types = ["page"]
parameter_schema = { title = "string", body = "string", representation = "string?", space_id = "string?", parent_id = "string?", before = "object?", output_name = "string?" }
description = "Render a local Confluence page-create proposal."

[capabilities."confluence.page.read"]
enabled = true
authentication = "configured_connector"
risk = "read_only"

[capabilities."confluence.page.search"]
enabled = true
authentication = "configured_connector"
risk = "read_only"

[capabilities."confluence.page.update"]
enabled = true
authentication = "configured_connector"
risk = "reversible_write"
reversible = true
requires_expected_version = true
provider_precondition = "version"
target_resource_types = ["page"]
parameter_schema = { title = "string", body = "string", representation = "string?", status = "string?", space_key = "string?", version_message = "string?", source_bindings = "object?" }
description = "Version-check and update a Confluence page."

[capabilities."confluence.page.update.draft"]
enabled = true
authentication = "local"
risk = "local_generation"
max_input_bytes = 4194304
max_output_bytes = 16777216
target_resource_types = ["page"]
parameter_schema = { title = "string", body = "string", representation = "string?", space_id = "string?", parent_id = "string?", before = "object?", output_name = "string?" }
description = "Render a local Confluence page-update proposal."

[capabilities."confluence.space.create"]
enabled = true
authentication = "configured_connector"
risk = "reversible_write"
reversible = true
target_resource_types = ["space"]
parameter_schema = { key = "string", name = "string", description = "string?" }
description = "Create and verify a Confluence Cloud space; deletion recovery requires manual re-review because the adapter lacks atomic delete preconditions."

[capabilities."github.checks.read"]
enabled = true
authentication = "configured_connector"
risk = "read_only"

[capabilities."github.collaborator.access.update"]
enabled = false
authentication = "configured_connector"
risk = "high_impact"
reversible = false
description = "Disabled because GitHub does not document a provider-side compare-and-swap precondition for this mutation."

[capabilities."github.issue.create"]
enabled = true
authentication = "configured_connector"
risk = "reversible_write"
reversible = true
target_resource_types = ["issue"]
parameter_schema = { owner = "string", repository = "string", title = "string", body = "string?" }
description = "Create and verify a GitHub issue; recovery requires a manual re-read and close because the adapter lacks atomic close preconditions."

[capabilities."github.pull_request.create"]
enabled = true
authentication = "configured_connector"
risk = "reversible_write"
reversible = true
target_resource_types = ["pull_request"]
parameter_schema = { owner = "string", repository = "string", title = "string", head = "string", base = "string", body = "string?", draft = "boolean?" }
description = "Create and verify a GitHub pull request; recovery requires a manual re-read and close because the adapter lacks atomic close preconditions."

[capabilities."github.pull_request.read"]
enabled = true
authentication = "configured_connector"
risk = "read_only"

[capabilities."github.pull_request.search"]
enabled = true
authentication = "configured_connector"
risk = "read_only"

[capabilities."github.public_repository.list"]
enabled = true
authentication = "anonymous_or_configured_connector"
risk = "read_only"
description = "List public repositories owned by a specified GitHub user without requiring credentials."

[capabilities."github.repository.list"]
enabled = true
authentication = "configured_connector"
risk = "read_only"
description = "List repositories visible to the authenticated GitHub user."

[capabilities."github.repository.read"]
enabled = true
authentication = "configured_connector"
risk = "read_only"

[capabilities."github.repository.settings.update"]
enabled = false
authentication = "configured_connector"
risk = "reversible_write"
reversible = true
requires_expected_version = true
description = "Disabled because GitHub does not document a provider-side compare-and-swap precondition for repository settings updates."

[capabilities."identity.identifier.resolve"]
enabled = true
authentication = "local"
risk = "read_only"

[capabilities."identity.person.list"]
enabled = true
authentication = "local"
risk = "read_only"

[capabilities."identity.person.resolve"]
enabled = true
authentication = "local"
risk = "read_only"

[capabilities."jira.issue.comment.create"]
enabled = true
authentication = "configured_connector"
risk = "reversible_write"
reversible = true
target_resource_types = ["issue"]
parameter_schema = { body = "string" }
description = "Create a Jira comment; deletion recovery requires manual re-review because the adapter lacks atomic delete preconditions."

[capabilities."jira.issue.comment.draft"]
enabled = true
authentication = "local"
risk = "local_generation"
max_input_bytes = 4194304
max_output_bytes = 16777216
target_resource_types = ["issue"]
parameter_schema = { body = "string", before = "object?", output_name = "string?" }
description = "Render a local Jira comment proposal."

[capabilities."jira.issue.compensate"]
enabled = false
authentication = "configured_connector"
risk = "reversible_write"
reversible = true
requires_expected_version = true
description = "Disabled until Jira exposes or the adapter implements a provider-side compare-and-swap precondition."

[capabilities."jira.issue.read"]
enabled = true
authentication = "configured_connector"
risk = "read_only"

[capabilities."jira.issue.search"]
enabled = true
authentication = "configured_connector"
risk = "read_only"

[capabilities."jira.issue.transition"]
enabled = false
authentication = "configured_connector"
risk = "reversible_write"
reversible = true
requires_expected_version = true
description = "Disabled until Jira exposes or the adapter implements a provider-side compare-and-swap precondition."

[capabilities."jira.issue.transition.draft"]
enabled = true
authentication = "local"
risk = "local_generation"
max_input_bytes = 4194304
max_output_bytes = 16777216
target_resource_types = ["issue"]
parameter_schema = { transition_id = "string", target_status = "string?", before = "object?", output_name = "string?" }
description = "Render a local Jira transition proposal."

[capabilities."jira.issue.update"]
enabled = false
authentication = "configured_connector"
risk = "reversible_write"
reversible = true
requires_expected_version = true
description = "Disabled until Jira exposes or the adapter implements a provider-side compare-and-swap precondition."

[capabilities."jira.issue.update.draft"]
enabled = true
authentication = "local"
risk = "local_generation"
max_input_bytes = 4194304
max_output_bytes = 16777216
target_resource_types = ["issue"]
parameter_schema = { fields = "object", before = "object?", output_name = "string?" }
description = "Render a local Jira field-update proposal."

[capabilities."jira.server.info"]
enabled = true
authentication = "configured_connector"
risk = "read_only"

[capabilities."microsoft.identity.read"]
enabled = true
authentication = "delegated_or_explicit_user"
risk = "read_only"

[capabilities."microsoft.identity.search"]
enabled = true
authentication = "delegated_or_application"
risk = "read_only"

[capabilities."onenote.notebook.list"]
enabled = true
authentication = "delegated"
risk = "read_only"
description = "List delegated OneNote notebooks with bounded pagination."

[capabilities."onenote.page.list"]
enabled = true
authentication = "delegated"
risk = "read_only"
description = "List delegated OneNote pages with bounded pagination."

[capabilities."onenote.page.read"]
enabled = true
authentication = "delegated"
risk = "read_only"
description = "Read one delegated OneNote page as untrusted content."

[capabilities."onenote.section.list"]
enabled = true
authentication = "delegated"
risk = "read_only"
description = "List delegated OneNote sections with bounded pagination."

[capabilities."onenote.page.create"]
enabled = false
authentication = "delegated"
risk = "reversible_write"
reversible = true
description = "Disabled until provider-normalized HTML can be verified exactly."

[capabilities."onenote.page.update"]
enabled = false
authentication = "delegated"
risk = "reversible_write"
reversible = true
requires_expected_version = true
description = "Disabled until target-aware PATCH poststate can be proven exactly."

[capabilities."outlook.attachment.list"]
enabled = true
authentication = "microsoft_graph_mail"
risk = "read_only"

[capabilities."outlook.attachment.text.read"]
enabled = true
authentication = "microsoft_graph_mail"
risk = "read_only"

[capabilities."outlook.email.draft"]
enabled = true
authentication = "local"
risk = "local_generation"
max_input_bytes = 4194304
max_output_bytes = 16777216
target_resource_types = ["draft", "email_draft"]
parameter_schema = { to = "string_or_string_list", subject = "string", body = "string", cc = "string_or_string_list?", bcc = "string_or_string_list?", output_name = "string?" }

[capabilities."outlook.email.send"]
enabled = true
authentication = "delegated"
risk = "external_communication"
reversible = false
required_scopes = ["Mail.ReadWrite", "Mail.Send"]
target_resource_types = ["message", "correction_email"]
parameter_schema = { to = "string_or_string_list", subject = "string", body = "string", cc = "string_or_string_list?", bcc = "string_or_string_list?", content_type = "string?", identity = "string?" }
description = "Send exact approval-bound Outlook content after provider-draft verification."

[capabilities."outlook.mail_folder.list"]
enabled = true
authentication = "microsoft_graph_mail"
risk = "read_only"

[capabilities."outlook.message.read"]
enabled = true
authentication = "microsoft_graph_mail"
risk = "read_only"

[capabilities."outlook.message.search"]
enabled = true
authentication = "microsoft_graph_mail"
risk = "read_only"

[capabilities."powerpoint.presentation.generate"]
enabled = true
authentication = "local"
risk = "local_generation"
max_input_bytes = 4194304
max_output_bytes = 16777216
target_resource_types = ["presentation"]
parameter_schema = { title = "string", slides = "array?", sections = "array?", subtitle = "string?", output_name = "string?", source_action_ids = "string_list?" }

[capabilities."repository.branch.create"]
enabled = false
authentication = "local_git"
risk = "reversible_write"
reversible = true
requires_expected_version = true
description = "Disabled until every local Git metadata transaction is descriptor-bound."

[capabilities."repository.branch.plan"]
enabled = true
authentication = "local"
risk = "local_generation"
max_input_bytes = 4194304
max_output_bytes = 16777216
target_resource_types = ["branch_plan"]
parameter_schema = { branch = "string", base = "string", remote = "string?", output_name = "string?" }
description = "Generate a local branch-operation plan."

[capabilities."repository.branch.push"]
enabled = false
authentication = "local_git"
risk = "reversible_write"
reversible = false
requires_expected_version = true
description = "Disabled until every local Git metadata transaction is descriptor-bound."

[capabilities."repository.commit.create"]
enabled = false
authentication = "local_git"
risk = "reversible_write"
reversible = true
requires_expected_version = true
description = "Disabled until every local Git metadata transaction is descriptor-bound."

[capabilities."repository.patch.apply"]
enabled = false
authentication = "local_git"
risk = "reversible_write"
reversible = true
requires_expected_version = true
description = "Disabled until every local Git metadata transaction is descriptor-bound."

[capabilities."repository.patch.generate"]
enabled = true
authentication = "local"
risk = "local_generation"
max_input_bytes = 4194304
max_output_bytes = 16777216
target_resource_types = ["patch"]
parameter_schema = { relative_path = "string", before_text = "string?", after_text = "string?", output_name = "string?" }
description = "Generate a reviewable unified diff without changing a repository."

[capabilities."sharepoint.drive.children"]
enabled = true
authentication = "microsoft_graph"
risk = "read_only"

[capabilities."sharepoint.drive.list"]
enabled = true
authentication = "microsoft_graph"
risk = "read_only"

[capabilities."sharepoint.file.metadata.read"]
enabled = true
authentication = "microsoft_graph"
risk = "read_only"

[capabilities."sharepoint.file.text.read"]
enabled = true
authentication = "microsoft_graph"
risk = "read_only"

[capabilities."sharepoint.file.upload"]
enabled = false
authentication = "microsoft_graph"
risk = "reversible_write"
reversible = true
requires_expected_version = true
required_scopes = ["Sites.ReadWrite.All"]
target_resource_types = ["file"]
parameter_schema = { drive_id = "string", local_path = "string", local_sha256 = "string", content_type = "string?" }
description = "Disabled because the Graph small-file content endpoint does not document an atomic conditional precondition."

[capabilities."sharepoint.site.read"]
enabled = true
authentication = "microsoft_graph"
risk = "read_only"

[capabilities."sharepoint.site.search"]
enabled = true
authentication = "microsoft_graph"
risk = "read_only"

[capabilities."teams.channel.list"]
enabled = true
authentication = "microsoft_graph_teams"
risk = "read_only"

[capabilities."teams.channel.message.list"]
enabled = true
authentication = "microsoft_graph_teams"
risk = "read_only"

[capabilities."teams.channel.message.read"]
enabled = true
authentication = "microsoft_graph_teams"
risk = "read_only"

[capabilities."teams.channel.message.replies.list"]
enabled = true
authentication = "microsoft_graph_teams"
risk = "read_only"

[capabilities."teams.channel.message.reply"]
enabled = true
authentication = "delegated"
risk = "external_communication"
reversible = false
required_scopes = ["ChannelMessage.Read.All", "ChannelMessage.Send"]
target_resource_types = ["message"]
parameter_schema = { team_id = "string", channel_id = "string", parent_message_id = "string", body = "string", content_type = "string?", identity = "string?" }
description = "Reply with exact approval-bound content in a Teams channel thread."

[capabilities."teams.channel.message.send"]
enabled = true
authentication = "delegated"
risk = "external_communication"
reversible = false
required_scopes = ["ChannelMessage.Read.All", "ChannelMessage.Send"]
target_resource_types = ["message"]
parameter_schema = { team_id = "string", channel_id = "string", body = "string", content_type = "string?", identity = "string?" }
description = "Post exact approval-bound content to a Teams channel."

[capabilities."teams.chat.list"]
enabled = true
authentication = "microsoft_graph_teams"
risk = "read_only"

[capabilities."teams.chat.message.list"]
enabled = true
authentication = "microsoft_graph_teams"
risk = "read_only"

[capabilities."teams.chat.message.read"]
enabled = true
authentication = "microsoft_graph_teams"
risk = "read_only"

[capabilities."teams.chat.message.send"]
enabled = true
authentication = "delegated"
risk = "external_communication"
reversible = false
required_scopes = ["Chat.Read", "ChatMessage.Send"]
target_resource_types = ["message"]
parameter_schema = { chat_id = "string", body = "string", content_type = "string?", identity = "string?" }
description = "Post exact approval-bound content to an existing Teams chat."

[capabilities."teams.message.draft"]
enabled = true
authentication = "local"
risk = "local_generation"
max_input_bytes = 4194304
max_output_bytes = 16777216
target_resource_types = ["draft", "message", "message_draft"]
parameter_schema = { recipient_type = "string", recipient_id = "string", body = "string", output_name = "string?", source_bindings = "object?" }

[capabilities."teams.team.list"]
enabled = true
authentication = "microsoft_graph_teams"
risk = "read_only"

# Exact normalized provider envelopes enforced at the model-context boundary.
# `query` is intentionally omitted from returned context. The shared read-only
# connector evidence/security/citation envelope is bound by the runtime.

[read_result_contracts."bitbucket.build_status.read"]
schema = "master-agent/bitbucket-build-status@1"
resources = { statuses = "object_list", summary = "object" }
metadata = ["system", "deployment", "commit", "returned", "source_urls"]

[read_result_contracts."bitbucket.instance.read"]
schema = "master-agent/bitbucket-instance@1"
resources = { instance = "object" }
metadata = ["system", "deployment", "source_urls"]

[read_result_contracts."bitbucket.public_repository.list"]
schema = "master-agent/bitbucket-public-repositories@1"
resources = { repositories = "object_list" }
metadata = ["system", "deployment", "returned", "source_urls"]

[read_result_contracts."bitbucket.pull_request.diffstat"]
schema = "master-agent/bitbucket-diffstat@1"
resources = { changes = "object_list", summary = "object" }
metadata = ["system", "deployment", "pull_request_id", "returned", "source_urls"]

[read_result_contracts."bitbucket.pull_request.read"]
schema = "master-agent/bitbucket-pull-request@1"
resources = { pull_request = "object" }
metadata = ["system", "deployment", "source_urls"]

[read_result_contracts."bitbucket.pull_request.search"]
schema = "master-agent/bitbucket-pull-requests@1"
resources = { pull_requests = "object_list" }
metadata = ["system", "deployment", "repository", "returned", "source_urls"]

[read_result_contracts."bitbucket.repository.read"]
schema = "master-agent/bitbucket-repository@1"
resources = { repository = "object" }
metadata = ["system", "deployment", "source_urls"]

[read_result_contracts."confluence.page.read"]
schema = "master-agent/confluence-page@1"
resources = { page = "object" }
metadata = ["system", "deployment", "source_urls"]

[read_result_contracts."confluence.page.search"]
schema = "master-agent/confluence-pages@1"
resources = { pages = "object_list" }
metadata = ["system", "deployment", "returned", "source_urls"]

[read_result_contracts."github.checks.read"]
schema = "master-agent/github-checks@1"
resources = { checks = "object_list", head_sha = "value", summary = "object" }
metadata = ["system", "deployment", "repository", "ref", "returned", "source_urls"]

[read_result_contracts."github.public_repository.list"]
schema = "master-agent/github-public-repositories@1"
resources = { repositories = "object_list" }
metadata = ["system", "deployment", "returned", "source_urls"]

[read_result_contracts."github.pull_request.read"]
schema = "master-agent/github-pull-request@1"
resources = { pull_request = "object" }
metadata = ["system", "deployment", "repository", "source_urls"]

[read_result_contracts."github.pull_request.search"]
schema = "master-agent/github-pull-requests@1"
resources = { pull_requests = "object_list" }
metadata = ["system", "deployment", "repository", "returned", "source_urls"]

[read_result_contracts."github.repository.list"]
schema = "master-agent/github-repositories@1"
resources = { repositories = "object_list" }
metadata = ["system", "deployment", "returned", "source_urls"]

[read_result_contracts."github.repository.read"]
schema = "master-agent/github-repository@1"
resources = { repository = "object" }
metadata = ["system", "deployment", "source_urls"]

[read_result_contracts."identity.identifier.resolve"]
schema = "master-agent/identity-identifier@1"
resources = { identifier = "value", person = "object" }
metadata = ["system", "target_system", "retention", "source_urls"]

[read_result_contracts."identity.person.list"]
schema = "master-agent/identity-people@1"
resources = { people = "object_list" }
metadata = ["system", "returned", "retention", "source_urls"]

[read_result_contracts."identity.person.resolve"]
schema = "master-agent/identity-person@1"
resources = { person = "object" }
metadata = ["system", "retention", "source_urls"]

[read_result_contracts."jira.issue.read"]
schema = "master-agent/jira-issue@1"
resources = { issue = "object" }
metadata = ["system", "deployment", "source_urls"]

[read_result_contracts."jira.issue.search"]
schema = "master-agent/jira-issues@1"
resources = { issues = "object_list" }
metadata = ["system", "deployment", "total", "returned", "source_urls"]

[read_result_contracts."jira.server.info"]
schema = "master-agent/jira-server@1"
resources = { reachable = "value", base_url = "value", version = "value", deployment_type = "value" }
metadata = ["deployment"]

[read_result_contracts."microsoft.identity.read"]
schema = "master-agent/microsoft-identity@1"
resources = { identity = "object" }
metadata = ["system", "retention", "source_urls"]

[read_result_contracts."microsoft.identity.search"]
schema = "master-agent/microsoft-identities@1"
resources = { users = "object_list" }
metadata = ["system", "returned", "retention", "source_urls"]

[read_result_contracts."onenote.notebook.list"]
schema = "master-agent/onenote-notebooks@1"
resources = { notebooks = "object_list" }
metadata = ["system", "identity", "returned", "retention", "source_urls"]

[read_result_contracts."onenote.page.list"]
schema = "master-agent/onenote-pages@1"
resources = { pages = "object_list" }
metadata = ["system", "identity", "returned", "retention", "source_urls"]

[read_result_contracts."onenote.page.read"]
schema = "master-agent/onenote-page@1"
resources = { page = "object" }
metadata = ["system", "identity", "retention", "source_urls"]

[read_result_contracts."onenote.section.list"]
schema = "master-agent/onenote-sections@1"
resources = { sections = "object_list" }
metadata = ["system", "identity", "returned", "retention", "source_urls"]

[read_result_contracts."outlook.attachment.list"]
schema = "master-agent/outlook-attachments@1"
resources = { attachments = "object_list" }
metadata = ["system", "identity", "message_id", "returned", "retention", "source_urls"]

[read_result_contracts."outlook.attachment.text.read"]
schema = "master-agent/outlook-text-attachment@1"
resources = { attachment = "object", content = "value", content_excerpt = "value" }
metadata = ["system", "identity", "message_id", "retention", "source_urls"]

[read_result_contracts."outlook.mail_folder.list"]
schema = "master-agent/outlook-mail-folders@1"
resources = { folders = "object_list" }
metadata = ["system", "identity", "parent_folder_id", "returned", "retention", "source_urls"]

[read_result_contracts."outlook.message.read"]
schema = "master-agent/outlook-message@1"
resources = { message = "object" }
metadata = ["system", "identity", "retention", "source_urls"]

[read_result_contracts."outlook.message.search"]
schema = "master-agent/outlook-messages@1"
resources = { messages = "object_list" }
metadata = ["system", "identity", "folder_id", "returned", "retention", "source_urls"]

[read_result_contracts."sharepoint.drive.children"]
schema = "master-agent/sharepoint-drive-items@1"
resources = { items = "object_list" }
metadata = ["system", "drive_id", "parent_item_id", "returned", "source_urls"]

[read_result_contracts."sharepoint.drive.list"]
schema = "master-agent/sharepoint-drives@1"
resources = { drives = "object_list" }
metadata = ["system", "site_id", "returned", "source_urls"]

[read_result_contracts."sharepoint.file.metadata.read"]
schema = "master-agent/sharepoint-file-metadata@1"
resources = { file = "object" }
metadata = ["system", "source_urls"]

[read_result_contracts."sharepoint.file.text.read"]
schema = "master-agent/sharepoint-text-file@1"
resources = { file = "object", content = "value", content_excerpt = "value" }
metadata = ["system", "source_urls"]

[read_result_contracts."sharepoint.site.read"]
schema = "master-agent/sharepoint-site@1"
resources = { site = "object" }
metadata = ["system", "source_urls"]

[read_result_contracts."sharepoint.site.search"]
schema = "master-agent/sharepoint-sites@1"
resources = { sites = "object_list" }
metadata = ["system", "returned", "source_urls"]

[read_result_contracts."teams.channel.list"]
schema = "master-agent/teams-channels@1"
resources = { channels = "object_list" }
metadata = ["system", "team_id", "returned", "retention", "source_urls"]

[read_result_contracts."teams.channel.message.list"]
schema = "master-agent/teams-channel-messages@1"
resources = { messages = "object_list" }
metadata = ["system", "team_id", "channel_id", "returned", "retention", "source_urls"]

[read_result_contracts."teams.channel.message.read"]
schema = "master-agent/teams-channel-message@1"
resources = { message = "object" }
metadata = ["system", "team_id", "channel_id", "retention", "source_urls"]

[read_result_contracts."teams.channel.message.replies.list"]
schema = "master-agent/teams-channel-message-replies@1"
resources = { messages = "object_list" }
metadata = ["system", "team_id", "channel_id", "parent_message_id", "returned", "retention", "source_urls"]

[read_result_contracts."teams.chat.list"]
schema = "master-agent/teams-chats@1"
resources = { chats = "object_list" }
metadata = ["system", "identity", "returned", "members_may_be_truncated", "retention", "source_urls"]

[read_result_contracts."teams.chat.message.list"]
schema = "master-agent/teams-chat-messages@1"
resources = { messages = "object_list" }
metadata = ["system", "chat_id", "returned", "retention", "source_urls"]

[read_result_contracts."teams.chat.message.read"]
schema = "master-agent/teams-chat-message@1"
resources = { message = "object" }
metadata = ["system", "chat_id", "retention", "source_urls"]

[read_result_contracts."teams.team.list"]
schema = "master-agent/teams-teams@1"
resources = { teams = "object_list" }
metadata = ["system", "identity", "returned", "retention", "source_urls"]
