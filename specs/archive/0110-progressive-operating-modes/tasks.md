# Tasks

- [x] Define the operating-mode and one-command requirement deltas and final
  current-requirement snapshots.
- [x] Implement strict organization-profile loading, private setup, and
  capability-scoped doctor results.
- [x] Implement high-level plan execution and approval resume over the existing
  direct-read and applied-run paths.
- [x] Bind the exact organization profile into effect execution and approval
  handoff.
- [x] Add adversarial profile, employee/developer boundary, readiness, CLI,
  provider-selection, approval, and effect-gate tests.
- [x] Add the reviewed development profile and dedicated semantic ownership
  route.
- [x] Update onboarding, CLI, configuration, architecture, operations, and
  agent guidance.
- [x] Run focused tests, type checks, formatting, semantic validation,
  `python scripts/specs.py validate`, and `python scripts/validate_release.py`.
- [x] Apply the final documentation maintenance gate to implementation and test
  evidence.
- [x] Move the change to verifying, archive it, and inspect the final diff.
