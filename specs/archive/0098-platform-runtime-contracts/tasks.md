# Tasks

- [x] Define the platform-runtime requirement and operating-mode modification
  snapshots.
- [x] Implement deterministic backend identity, availability, and fail-closed
  selection without import-time Unix dependencies.
- [x] Integrate platform status with package imports, CLI help/version,
  deployment readiness, and progressive doctor output.
- [x] Keep stateful draft/effect, retention, process, Git, and capsule paths
  unavailable until their exact secure backend exists.
- [x] Add Windows import/configuration-only coverage and unchanged POSIX
  regression coverage.
- [x] Add exact semantic ownership for the released common platform-runtime
  route while preserving every native Windows route as planned.
- [x] Update architecture, threat model, roadmap, CLI, release, onboarding, and
  historical release documentation.
- [x] Run focused tests, type checks, formatting, semantic validation,
  `python scripts/specs.py validate`, and `python scripts/validate_release.py`.
- [x] Apply the final documentation maintenance gate to implementation and test
  evidence.
- [x] Move the change to verifying, archive it, and inspect the final diff.
