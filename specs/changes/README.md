# Active specification changes

Create one normalized subdirectory per non-trivial behavioral change. Use the
files in `specs/templates/` and archive verified changes with
`python scripts/specs.py archive <change-id>`.
